# Tests expected to be skipped in e2e runs.
#
# One test method name per line (as reported by NUnit3TestAdapter).
# Lines starting with # are comments; blank lines are ignored.
#
# When the emitter implements a scenario and a test starts passing,
# remove it from this file. The e2e run will fail if:
#   1. A test is skipped that is NOT listed here (unexpected skip)
#   2. A test listed here actually passes (stale entry — remove it)

# --- Encode/Duration: Skipped by SpectorTestAttribute (not yet implemented) ---
HeaderFloatMillisecondsLargerUnit
HeaderFloatSecondsLargerUnit
QueryFloatMillisecondsLargerUnit
QueryFloatSecondsLargerUnit

# --- Documentation: BulletPointsModel uses BulletPointsEnum not yet generated correctly ---
BulletPointsModel
